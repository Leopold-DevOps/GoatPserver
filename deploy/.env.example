# WireGuard IP of the machine running these containers.
# Find it on that box with:  ip -4 addr show wg0
# Clients are told to open the game socket at this address, so it must be
# reachable from your PC over the VPN - not 127.0.0.1, not the LAN IP.
SERVER_ADDRESS=10.0.0.1
