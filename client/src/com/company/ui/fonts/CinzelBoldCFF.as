package com.company.ui.fonts {
import mx.core.*;

/* Cinzel (SIL Open Font License) - see Cinzel-OFL.txt in this folder.
   Instanced from Google's variable Cinzel[wght].ttf: mxmlc cannot embed a
   variable font, so Regular (wght 400) and Bold (wght 700) are static cuts. */
[Embed(source="Cinzel-Bold.ttf", fontName="CinzelBoldCFF", fontFamily="Cinzel", fontStyle="Bold", fontWeight="bold", embedAsCFF="true", mimeType="application/x-font")]
public class CinzelBoldCFF extends FontAsset {
    public function CinzelBoldCFF() {
        super();
    }
}
}
